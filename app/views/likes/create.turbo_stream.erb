<%= turbo_stream.replace @post, 
                        partial: "posts/post",
                        locals: {
                            post: @post,
                            liked: true,
                            likes_count: @post.likes.count
                        }  %>